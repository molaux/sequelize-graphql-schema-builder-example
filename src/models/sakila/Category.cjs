/**
 * Auto generated by MySQL Workbench Schema Exporter.
 * Version 3.1.5-dev (node-Sequelize6 dev) on 2021-03-08 17:10:02.
 * Goto
 * https://github.com/mysql-workbench-schema-exporter/mysql-workbench-schema-exporter
 * for more information.
 */

const { DataTypes, Model } = require('sequelize')

class Category extends Model {
}

module.exports = (sequelize, extend) => {
  Category.init(extend({
    categoryId: {
      type: DataTypes.SMALLINT,
      field: 'category_id',
      primaryKey: true,
      autoIncrement: true
    },
    name: {
      type: DataTypes.STRING(25),
      field: 'name',
      allowNull: false
    },
    lastUpdate: {
      type: DataTypes.DATE,
      field: 'last_update',
      allowNull: false,
      defaultValue: sequelize.literal('CURRENT_TIMESTAMP')
    }
  }), {
    sequelize: sequelize,
    modelName: 'Category',
    tableName: 'category',
    timestamps: false,
    underscored: true,
    syncOnAssociation: false
  })

  Category.associate = () => {
    // N <=> M association
    Category.belongsToMany(sequelize.models.Film, {
      through: 'film_category',
      foreignKey: {
        name: 'category_id'
      },
      onUpdate: 'CASCADE',
      onDelete: 'RESTRICT',
      targetKey: 'filmId',
      as: 'Films'
    })
  }

  return Category
}
