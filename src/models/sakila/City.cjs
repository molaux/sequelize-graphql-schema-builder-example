/**
 * Auto generated by MySQL Workbench Schema Exporter.
 * Version 3.1.5-dev (node-Sequelize6 dev) on 2021-04-13 19:01:50.
 * Goto
 * https://github.com/mysql-workbench-schema-exporter/mysql-workbench-schema-exporter
 * for more information.
 */

const { DataTypes, Model } = require('sequelize')

class City extends Model {
}

module.exports = (sequelize, extend) => {
  City.init(extend({
    cityId: {
      type: DataTypes.SMALLINT,
      field: 'city_id',
      primaryKey: true,
      autoIncrement: true
    },
    city: {
      type: DataTypes.STRING(50),
      field: 'city',
      allowNull: false
    },
    lastUpdate: {
      type: DataTypes.DATE,
      field: 'last_update',
      allowNull: false,
      defaultValue: sequelize.literal('CURRENT_TIMESTAMP')
    }
  }), {
    sequelize: sequelize,
    modelName: 'City',
    tableName: 'city',
    indexes: [
      {
        name: 'idx_fk_country_id',
        fields: ['country_id']
      }
    ],
    timestamps: false,
    underscored: true,
    syncOnAssociation: false
  })

  City.associate = () => {
    // 1 <=> N association
    City.hasMany(sequelize.models.Address, {
      foreignKey: {
        name: 'cityId',
        allowNull: false
      },
      onUpdate: 'CASCADE',
      onDelete: 'RESTRICT',
      targetKey: 'cityId',
      as: 'Addresses'
    })

    // N <=> 1 association
    City.belongsTo(sequelize.models.Country, {
      foreignKey: {
        name: 'countryId',
        allowNull: false
      },
      onUpdate: 'RESTRICT',
      targetKey: 'countryId'
    })
  }

  return City
}
